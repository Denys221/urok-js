"use strict";

/**
 * Функция конвертирует рубли в другую валюту
 * @param {number} roubles величина в рублях
 * @param {string} currency поддержуются значения "dollar" и "euro"
 * @throws {Error} если передана не поддержываемая валюта, то будет
 *                 выброшена ошибка
 * @returns {number}
 */

/*  function convertCurrency(roubles, currency) {
    let currencyCost = 0;
    switch (currency) {
      case "dollar":
       currencyCost = 66;
       break;
      case "euro":
      currency = 75;
        break;
    }

   let result = roubles / currencyCost;
   return result;
}
 let inDollars = convertCurrency(100, "dollar");
 console.log("Ваша сумма в долларах: " + inDollars);*/


 /*let a = 1, b = 1, c, d;
c = ++a;
alert(c); //префиксная форма

d = b++;
alert(d); //постфиксная форма

c = 2 + ++a; //префиксная форма
alert(c);

d = 2 + b++; //постфиксная форма
alert(d);

alert(a);
alert(b);*/

/*let a = 2;
let x = 1 +(a *= 2); // умножаю сначала содержимое в душках a(2) на 2 = 4; потом плюсую 1 + 4 = 5;
alert(x); */           // a = 4; x = 5;


/*let a = 3;
let b = 5;
if (a >= 0 && b >= 0) {
  alert(a - b);
} else if (a < 0 && b < 0) {
  alert(a * b);
} else if (a < 0 && b > 0 || a > 0 && b < 0); {
  alert(a + b);
}*/

//
function plus (a, b); {
  return a + b;
}
console.log(plus(13, 13));

function minus (a, b); {
  return a - b;
}
console.log(minus(23, 1024));

function delenie (a, b); {
  return a / b;
}
function umnozenie (a, b); {
  return a * b;

}*/
